name: Diagnostics relay

on:
  pull_request:
    paths: ['diagnostics-relay/**', '.github/workflows/diagnostics-relay.yml']
  push:
    branches: [main]
    paths: ['diagnostics-relay/**', '.github/workflows/diagnostics-relay.yml']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: diagnostics-relay
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: diagnostics-relay/package-lock.json
      - run: npm ci
      - run: npm run check
